[build-system]
requires = ["setuptools>=68,<81"]
build-backend = "setuptools.build_meta"

[project]
name = "auth-log-analyzer"
version = "0.3.0"
description = "Parse synthetic authentication logs and summarize defensive indicators locally."
readme = "README.md"
requires-python = ">=3.11"
license = { file = "LICENSE" }
authors = [{ name = "Oscar Opemba" }]
classifiers = [
  "Development Status :: 4 - Beta",
  "Intended Audience :: Information Technology",
  "License :: OSI Approved :: MIT License",
  "Programming Language :: Python :: 3",
  "Programming Language :: Python :: 3 :: Only",
]

[project.urls]
Repository = "https://github.com/Oscar-Opemba/auth-log-analyzer"
Issues = "https://github.com/Oscar-Opemba/auth-log-analyzer/issues"

[project.scripts]
auth-log-analyzer = "app:main"

[tool.setuptools]
py-modules = ["app", "common", "security_utils"]
